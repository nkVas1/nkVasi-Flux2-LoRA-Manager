# 🚀 QUICK START v1.1 - Обновлённая версия с веб-логами

## ⚡ Что нового

✅ **Логи теперь видны в браузере** - зелёная панель справа снизу  
✅ **Исправлены ошибки Windows** - accelerate теперь работает надёжнее  
✅ **Реальное время** - логи появляются по мере выполнения  
✅ **Цветное выделение** - ошибки красные, успех зелёный и т.д.

---

## 📦 Установка обновления

Если у вас уже установлена версия 1.0, просто обновите файлы:

### Шаг 1: Обновите из Git
```powershell
cd "ComfyUI/custom_nodes/ComfyUI-Flux2-LoRA-Manager"
git pull origin main
```

### Шаг 2: Перезагрузите ComfyUI (КРИТИЧНО!)
```powershell
# В окне, где запущен ComfyUI нажмите Ctrl+C
# Затем заново запустите ComfyUI
```

### Шаг 3: Обновите браузер
```
Нажмите Ctrl+Shift+R (Hard Refresh) чтобы очистить кеш
```

---

## 🎯 Использование

### Создаём workflow:

```
┌─────────────────────────────────────┐
│  🛠️ FLUX.2 Config (Low VRAM)        │
│  ────────────────────────────────   │
│  sd_scripts_path:                   │
│  C:/AI/sd-scripts                   │
│                                     │
│  model_path:                        │
│  black-forest-labs/FLUX.1-dev       │
│                                     │
│  img_folder:                        │
│  C:/Dataset/my_images               │
│                                     │
│  output_name:                       │
│  my_character_lora                  │
│                                     │
│  resolution: 512                    │
│  lora_rank: 16                      │
│  max_train_steps: 1200              │
└──────────┬──────────────────────────┘
           │ cmd_args
           ↓
┌──────────────────────────────────────┐
│  🚀 Start Training (External)        │
│  ────────────────────────────────    │
│  trigger: [Toggle to TRUE]           │
└──────────────────────────────────────┘
```

### Нажимаем "Queue Prompt"

```
1. ⏳ Ноды выполняются в ComfyUI
2. 🟢 Справа снизу появляется зелёная панель
3. 📊 Логи "kohya_ss" бегут в реальном времени
4. ✅ После завершения видим сообщение о успехе
```

---

## 🖥️ Пример того, что вы увидите в браузере

```
🚀 FLUX.2 Training Monitor

=== FLUX TRAIN LOG STARTED ===
[FLUX-TRAIN] Loading model: black-forest-labs/FLUX.1-dev
[FLUX-TRAIN] Model loaded successfully
[FLUX-TRAIN] Loading 42 images from C:/Dataset/my_images
[FLUX-TRAIN] Initializing LoRA (rank=16, alpha=16)
[FLUX-TRAIN] Starting training loop...
[FLUX-TRAIN] Step 1/1200: loss=2.543 (lr=0.0001) **← ГОЛУБОЙ ТЕКСТ**
[FLUX-TRAIN] Step 2/1200: loss=2.341
[FLUX-TRAIN] Step 3/1200: loss=2.125
...
[FLUX-TRAIN] Step 600/1200: loss=0.543
[FLUX-TRAIN] Saving checkpoint...
[FLUX-TRAIN] Step 1200/1200: loss=0.234
[FLUX-TRAIN] Training finished successfully! **← ЗЕЛЁНЫЙ ТЕКСТ**

✅ TRAINING COMPLETED
```

**Цветовая легенда:**
- 🟢 Зелёный = успех, завершение
- 🔴 Красный = ошибки, CUDA OOM
- 🟡 Жёлтый = warning
- 🔵 Голубой = информация (steps, loss)
- 🟠 Оранжевый = подготовка (loading, preparing)

---

## ⚙️ Что изменилось в коде

### Файлы, которые изменились:
1. ✏️ `__init__.py` - добавлена строка `WEB_DIRECTORY = "./js"`
2. ✏️ `src/process.py` - добавлено WebSocket логирование
3. ✏️ `src/config_gen.py` - исправлен вызов accelerate для Windows
4. 🆕 `js/flux_monitor.js` - новый файл с веб-интерфейсом

### Файлы без изменений:
- `nodes.py` - логика нод не изменилась
- `requirements.txt` - зависимости те же
- README.md, LICENSE и т.д.

---

## 🔧 Если что-то не работает

### ❌ Панель не появляется
**Решение:**
1. Откройте DevTools браузера (F12)
2. Перейдите на вкладку "Console"
3. Ищите ошибки JavaScript
4. Нажмите Ctrl+Shift+R для полной переезагрузки браузера

### ❌ "accelerate: command not found"
**Решение:**
- Убедитесь, что accelerate установлен:
  ```powershell
  pip install accelerate
  ```
- Проверьте, что вы используете правильное окружение
- Смотрите логи в консоли сервера (чёрное окно ComfyUI)

### ❌ Логи пропадают / панель есть, но пуста
**Решение:**
1. Проверьте консоль сервера (чёрное окно) - там должны быть `[FLUX-TRAIN]` логи
2. Если там пусто - процесс не запустился, смотрите ошибку
3. Если там есть логи - это проблема WebSocket, перезагрузите браузер

---

## 📊 Сравнение версий

| Функция | v1.0 | v1.1 |
|---------|------|------|
| Базовые ноды | ✅ | ✅ |
| Логи в консоли | ✅ | ✅ |
| **Логи в браузере** | ❌ | ✅ |
| **Веб-интерфейс** | ❌ | ✅ |
| **Цветное выделение** | ❌ | ✅ |
| **Windows fix** | ❌ | ✅ |
| Real-time streaming | ❌ | ✅ |

---

## 🎓 Технические детали

### WebSocket архитектура

```
Browser (js/flux_monitor.js)
    ↓ api.addEventListener("flux_train_log")
ComfyUI Server (PromptServer)
    ↓ send_sync("flux_train_log", data)
Python Thread (_log_reader)
    ↓ subprocess.stdout.readline()
kohya_ss Training Process
```

### Почему это лучше?

**Было (v1.0):**
- Логи в консоли сервера (чёрное окно)
- Нужно держать открытым терминал
- Неудобно на удалённых серверах
- Не видно в веб-интерфейсе

**Теперь (v1.1):**
- Логи в браузере (зелёная панель)
- Всё видно из веб-интерфейса ComfyUI
- Удобно на любых устройствах
- Красиво и функционально

---

## 📝 Git история

```
Commit 3: v1.1: Add web UI monitoring, WebSocket logging, and Windows fixes
├── Add flux_monitor.js: Real-time log visualization
├── Update process.py: WebSocket streaming
├── Update config_gen.py: Windows compatibility
└── Update __init__.py: Register JS extension

Commit 2: Add comprehensive GitHub setup and deployment guide
├── Add GITHUB_SETUP.md

Commit 1: Initial commit: ComfyUI FLUX.2 LoRA Manager for 8GB VRAM
├── Full project structure
├── Three core nodes
└── Complete documentation
```

---

## 🚀 Следующие шаги

### Для локального тестирования:
1. ✅ Обновьте ComfyUI
2. ✅ Перезагрузите браузер
3. ✅ Создайте тестовый workflow
4. ✅ Нажмите Queue и смотрите логи в браузере

### Для публикации обновления:
```powershell
cd "g:\CODING\nkVasi_Flux2_LoRA_LowVRAM\ComfyUI-Flux2-LoRA-Manager"
git push origin main
```

### Для распространения:
- Поделитесь ссылкой в ComfyUI сообществах
- Добавьте в реестр ComfyUI node packs
- Создайте пост о нововведениях

---

## ✅ Финальный чек-лист

Перед использованием убедитесь:

- [ ] ComfyUI перезагружен
- [ ] Браузер обновлён (Ctrl+Shift+R)
- [ ] Файл `js/flux_monitor.js` существует
- [ ] `__init__.py` содержит `WEB_DIRECTORY = "./js"`
- [ ] `accelerate` установлен (`pip list | grep accelerate`)
- [ ] Путь к sd-scripts правильный
- [ ] Датасет имеет картинки и .txt файлы с подписями

---

## 💬 Feedback & Issues

Если что-то не работает:
1. Смотрите консоль браузера (F12 → Console)
2. Смотрите консоль сервера (чёрное окно ComfyUI)
3. Проверьте GITHUB_SETUP.md для troubleshooting
4. Создайте Issue на GitHub с логами ошибки

---

**Версия:** 1.1  
**Статус:** Production-Ready ✅  
**Дата выпуска:** 2025-01-15
