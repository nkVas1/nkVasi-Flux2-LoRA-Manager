# 🚀 FLUX.2 LoRA Manager v1.2 - ОБНОВЛЕНИЕ С УЛУЧШЕНИЯМИ

## ✅ ЧТО ИЗМЕНИЛОСЬ (Critical Fixes)

### 🎯 Основные улучшения:

1. **JavaScript панель логов в браузере**
   - Логи теперь видны прямо в ComfyUI интерфейсе (черное окно справа снизу)
   - Раскраска по типу сообщений (ошибки красные, прогресс голубой и т.д.)
   - Панель можно закрывать и перемещать

2. **Исправления Windows проблем**
   - Добавлен флаг `-u` для Python (unbuffered output)
   - Установлена переменная окружения `PYTHONIOENCODING=utf-8`
   - Используется `sys.executable` вместо прямого вызова `accelerate`

3. **Улучшенная обработка ошибок**
   - Перехват ошибок при запуске процесса
   - Отправка сообщений об ошибках в браузер
   - Логирование в консоль сервера И в браузер одновременно

4. **Лучшее логирование**
   - Стартовое сообщение с полной командой при запуске
   - Сообщения об ошибках видны в браузере в красном цвете
   - Прогресс-информация (steps, loss, percentage) в голубом цвете

---

## 📋 ФАЙЛЫ КОТОРЫЕ ИЗМЕНИЛИСЬ

```
✅ __init__.py                 - Добавлена строка WEB_DIRECTORY = "./js"
✅ js/flux_monitor.js          - Полностью переписан с улучшенной логикой
✅ src/process.py              - Улучшена обработка ошибок и логирование
✅ src/config_gen.py           - Добавлены флаги -u для unbuffered output
```

---

## 🔧 КАК УСТАНОВИТЬ ОБНОВЛЕНИЕ

### Шаг 1: Обновите файлы в ComfyUI

Если у вас уже установлен нодпак в `ComfyUI/custom_nodes/ComfyUI-Flux2-LoRA-Manager/`:

```bash
# Перейдите в папку проекта
cd ComfyUI/custom_nodes/ComfyUI-Flux2-LoRA-Manager

# Обновите все файлы (скопируйте обновленные версии)
```

### Шаг 2: ОБЯЗАТЕЛЬНО ОЧИСТИТЕ БРАУЗЕР-КЭШ

Это **КРИТИЧНО** для загрузки нового JS файла:

**Windows/Linux (Chrome/Firefox/Edge):**
- Нажмите `Ctrl + F5` (или `Cmd + Shift + R` на Mac)
- Или нажмите `Ctrl + Shift + Delete` и очистите кэш за "все время"

**Safari:**
- Нажмите `Cmd + Option + E` (очистить кэш)

### Шаг 3: Перезагрузите ComfyUI

```bash
# Если запущено в батнике:
# 1. Закройте окно ComfyUI
# 2. Запустите run_nvidia_gpu.bat заново
```

### Шаг 4: Проверьте загрузку расширения

1. Откройте браузер на `http://localhost:8188` (или ваш адрес)
2. Нажмите `F12` (откроется DevTools)
3. Перейдите на вкладку **Console**
4. Вы должны увидеть голубые сообщения:
   ```
   [FLUX Monitor] Loading Extension...
   [FLUX Monitor] Setup Started
   [FLUX Monitor] Ready and Listening!
   ```

Если этих сообщений нет - очистите кэш (Шаг 2) и перезагрузитесь.

---

## 🎮 КАК ИСПОЛЬЗОВАТЬ (Пошагово)

### 1️⃣ Подготовка датасета

Создайте папку с изображениями и подписями:
```
C:\Dataset\my_dataset\
├── image_001.jpg
├── image_001.txt     (описание изображения)
├── image_002.jpg
├── image_002.txt
└── ...
```

Каждый `.txt` файл содержит описание (caption) для соответствующего изображения.

### 2️⃣ Создание workflow в ComfyUI

1. Добавьте ноду **🛠️ FLUX.2 Config (Low VRAM)**
2. Заполните параметры:
   - `sd_scripts_path`: Путь к папке `sd-scripts` (где находится `flux_train_network.py`)
   - `model_path`: `black-forest-labs/FLUX.1-dev` (или путь к вашей модели)
   - `img_folder`: Путь к папке с датасетом
   - `output_name`: Имя для вашей LoRA (например, `my_style_lora`)
   - `resolution`: **512** (для 8GB рекомендуется начать с этого)
   - `max_train_steps`: 1200-1600
   - `lora_rank`: **16** (безопасно для 8GB)
   - `learning_rate`: `0.0001` (стандартный)

3. Нажмите на выход `cmd_args` и соедините с нодой **🚀 Start Training (External)**

4. В ноде **🚀 Start Training**:
   - `trigger`: установите на **True**

5. Нажмите **Run** (или **Queue Prompt** если у вас обычный ComfyUI)

### 3️⃣ Наблюдение процесса

✅ Справа снизу браузера **автоматически** всплывет черное окно с логами

Вы увидите в реальном времени:
- 🟡 **Жёлтый текст** - Процент выполнения, шаги обучения
- 🔵 **Голубой текст** - Loss, метрики обучения
- 🟠 **Оранжевый текст** - Подготовка данных
- 🔴 **Красный текст** - **Ошибки** (если что-то пошло не так)
- 🟢 **Зелёный текст** - Статус, завершение

### 4️⃣ Если процесс упал

Посмотрите красное сообщение об ошибке в панели логов:

**Частые ошибки:**

| Ошибка | Решение |
|--------|---------|
| `ModuleNotFoundError: No module named 'accelerate'` | Установите: `pip install accelerate` |
| `CUDA out of memory` | Уменьшите resolution на 512 или снизьте rank на 8 |
| `File not found: ...flux_train_network.py` | Проверьте путь `sd_scripts_path` |
| `ModuleNotFoundError: No module named 'bitsandbytes'` | Установите: `pip install bitsandbytes` |
| `AttributeError: 'NoneType' object` | Перезагрузите ComfyUI полностью |

---

## 🛑 ЭКСТРЕННАЯ ОСТАНОВКА

Если процесс зависнет или нужно остановить обучение:

1. Добавьте ноду **🛑 Emergency Stop**
2. Установите `stop` на **True**
3. Нажмите **Run**

Процесс получит сигнал SIGTERM (graceful shutdown), и если это не сработает, то SIGKILL (force kill).

---

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ (RTX 3060 Ti, 8GB)

| Параметр | Значение | Примечание |
|----------|----------|-----------|
| **Resolution** | 512x512 | Рекомендуется для первого запуска |
| **Batch Size** | 1 | Жесткое ограничение для 8GB |
| **Rank** | 16 | Balance между качеством и скоростью |
| **Steps** | 1200 | ≈ 35-45 минут на 20 изображениях |
| **VRAM Usage** | ≈7.2 GB | Остается место для буфера |
| **Тип оптимизатора** | Adafactor | Экономит ≈1GB по сравнению с AdamW |

### Если хватает памяти, можно повысить:
- **Resolution** → 768x768 (добавит ≈45 минут к времени обучения)
- **Rank** → 32 (улучшит качество, но медленнее)

---

## 🧪 ДИАГНОСТИКА

### Если логи НЕ видны в браузере:

1. **Откройте DevTools (F12) → Console**
   - Ищите сообщения `[FLUX Monitor]`
   - Если их нет → очистите кэш (Ctrl+F5) → перезагрузитесь

2. **Проверьте терминал ComfyUI (черное окно)**
   - Там должны быть сообщения `[FLUX-TRAIN]`
   - Если их нет → процесс не запустился, посмотрите ошибку выше

3. **Проверьте, существует ли файл `js/flux_monitor.js`**
   ```bash
   # На Windows в PowerShell:
   Test-Path "ComfyUI\custom_nodes\ComfyUI-Flux2-LoRA-Manager\js\flux_monitor.js"
   ```
   Должен вернуть `True`

4. **Проверьте, что WEB_DIRECTORY прописана в __init__.py**
   ```python
   WEB_DIRECTORY = "./js"
   ```

---

## 💾 ГДЕ НАХОДИТСЯ ГОТОВАЯ LoRA?

После успешного обучения LoRA файл сохранится в:

```
ComfyUI/output/flux_training/[your_output_name]/
```

Там вы найдёте:
- `latest.safetensors` - последняя сохранённая версия
- `train.log` - полный лог обучения
- `trainer_config.json` - конфиг использованный для обучения

---

## 📝 ОБНОВЛЕНИЕ ПРОЕКТА В GIT

Если хотите закоммитить эти улучшения:

```bash
cd ComfyUI-Flux2-LoRA-Manager

git add .
git commit -m "v1.2: Add browser logging panel, Windows fixes, unbuffered output

- Add JavaScript web extension for real-time logs in browser
- Fix Windows console output encoding (PYTHONIOENCODING=utf-8)
- Add Python unbuffered flag (-u) for streaming logs
- Improve error handling and startup messages
- Color-code log output by message type"

git push origin main
```

---

## 🎉 ГОТОВО!

Теперь ваш нодпак имеет:
- ✅ Полную видимость процесса в браузере
- ✅ Исправлены все проблемы Windows
- ✅ Красивую панель с логами
- ✅ Автоматическое раскрытие информативной панели

**Удачного обучения!** 🚀

Если возникнут проблемы - посмотрите раздел "🛑 Частые ошибки" или создайте Issue на GitHub.
